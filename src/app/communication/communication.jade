
.m-menu-documents(data-offset-top="100", bs-affix)
  .m-menu-documents-item(
  ng-click='vm.document.docType = item.value; vm.getNumberDocument()',
  ng-class='{"m-menu-document-item-active": false}')
    img.disabled(ng-src='../assets/images/help_desk/ticket.svg')
    img.active(ng-src='../assets/images/help_desk/ticket-reverse.svg')
    md-tooltip(md-direction="bottom") Новые заказы
  .m-menu-documents-item(
  ng-click='vm.document.docType = item.value; vm.getNumberDocument()',
  ng-class='{"m-menu-document-item-active": false}'
  )
    img.disabled(ng-src='../assets/images/help_desk/ticket.svg')
    img.active(ng-src='../assets/images/help_desk/ticket-reverse.svg')
    md-tooltip(md-direction="bottom") В процессе
  .m-menu-documents-item(
  ng-click='vm.document.docType = item.value; vm.getNumberDocument()',
  ng-class='{"m-menu-document-item-active": false}'
  )
    img.disabled(ng-src='../assets/images/help_desk/ticket.svg')
    img.active(ng-src='../assets/images/help_desk/ticket-reverse.svg')
    md-tooltip(md-direction="bottom") Закрытые
  .m-menu-documents-item(
  ng-click='vm.document.docType = item.value; vm.getNumberDocument()',
  ng-class='{"m-menu-document-item-active": false}'
  )
    img.disabled(ng-src='../assets/images/help_desk/ticket.svg')
    img.active(ng-src='../assets/images/help_desk/ticket-reverse.svg')
    md-tooltip(md-direction="bottom") Добавить новый

.m-menu-documents.m-menu-documents-right(data-offset-top="100", bs-affix)
  .m-menu-documents-item(ng-click='vm.sendRequestSelling()', ng-class='{animation: vm.getClassValidationForSubmit()}')
    img.disabled(ng-src='../assets/images/help_desk/backup.svg')
    img.active(ng-src='../assets/images/help_desk/backup-reverse.svg')
    md-tooltip(md-direction="bottom") Отправить
  .m-menu-documents-item(ui-sref='settings')
    img.disabled(ng-src='../assets/images/help_desk/settings.svg')
    img.active(ng-src='../assets/images/help_desk/settings-reverse.svg')
    md-tooltip(md-direction="bottom") Настройки
  .m-menu-documents-item(ng-click='vm.logout()')
    img.disabled(ng-src='../assets/images/help_desk/close.svg')
    img.active(ng-src='../assets/images/help_desk/close-reverse.svg')
    md-tooltip(md-direction="bottom") Закрыть

script.
  $(document).ready(function() {
  $(".autocomplete-div").click(function() {
  $(".autocomplete-text").css("display", "none");
  $(".autocomplete-input").css("display", "inline-block");
  $(".autocomplete-input").focus();
  });
  $(".selected-item-close").click(function() {
  $(this).parent().parent().parent().parent().parent().fadeOut(100);
  });
  });   
  $(".star").mouseenter(function() {
  $(this).get(0).className = 'fa fa-star star';
  });
  $(".star").mouseleave(function() {
  $(this).get(0).className = 'fa fa-star-o star';
  });
  $(".btn-select").click(function() {
  $(".btn-select").css({"background":"#f2f2f2", "color": "#333"});
  $(this).css({"background":"#f57373", "color": "#fff"});
  });
  $(".btn-select").mouseenter(function() {
  if("rgb(242, 242, 242)" == $(this).css("backgroundColor")) $(this).css({"background":"#f57374", "color": "#fff"});
  });
  $(".btn-select").mouseleave(function() {
  if("rgb(245, 115, 115)" != $(this).css("backgroundColor")) $(this).css({"background":"#f2f2f2", "color": "#333"});
  });

  // $(".m-flex").delegate(".lists tr", "click", function() {
  // $(".lists tr").css("backgroundColor", "#fff");
  // $(this).css("backgroundColor", "#f5e6a3");
  // });
  // $(".m-flex").delegate(".lists tr", "mouseenter", function() {
  // if(("rgb(245, 230, 163)" != $(this).css("backgroundColor")) && ("rgb(248, 238, 192)" != $(this).css("backgroundColor"))) $(this).css("backgroundColor", "#f5f5f5");
  // else $(this).css("backgroundColor", "#f5e6a3");
  // });
  // $(".m-flex").delegate(".lists tr", "mouseleave", function() {
  // if(("rgb(245, 230, 163)" != $(this).css("backgroundColor")) && ("rgb(248, 238, 192)" != $(this).css("backgroundColor"))) $(this).css("backgroundColor", "#fff");
  // else $(this).css("backgroundColor", "#f8eec0");
  // });
   
m-row.m-document.m-flex
  .m-left-body
    .m-row
      h1.text-header Список тикетов
      .btn-group
        p.btn-select(ng-click="filter('my')", style='background: #f57373;color:#fff;') Мои
        p.btn-select(ng-click="filter('common')") Общие
        p.btn-select(ng-click="filter('all')") Все
      // .btn-group.m-radio-group(ng-model='vm.selector', bs-radio-group='')
      //   label.btn.btn-default
      //     input.btn.btn-default(type='radio', value='my', checked='checked')
      //     |  Мои
      //   label.btn.btn-default
      //     input.btn.btn-default(type='radio', value='withMe')
      //     |  Общие
      //   label.btn.btn-default
      //     input.btn.btn-default(type='radio', value='all')
      //     |  Все
    .like-invoice-title
      .m-point.m-point-greendot
      | Количество заявок: {{vm.tickets.length}}
    .m-table.like-invoice-body-clear-left-top-padding
      .m-row(layout='row')
        .m-col(flex=5)
          input.m-checkbox.m-checkbox-normal(ng-click='$event.stopPropagation();', type="checkbox", ng-model='vm.allChecked', value="")
        .m-col(flex=5)
          .m-checkbox-start.m-checkbox-start-normal(
          ng-class="{'m-checkbox-checked-start':vm.allStar}",
          ng-click="$event.stopPropagation(); vm.allStar = !vm.allStar;")
        .m-col(flex)
          // employers
          button.m-select(  type='button', ng-model='vm.selectedEmployer', placeholder='', data-html='1', data-toggle='true', bs-options='item.value as item.label for item in vm.employers', bs-select='')
            span.m-selected-span.m-selected-face
            span.caret

          // statuses
          button.m-select(  type='button', ng-model='vm.selectedStatus', placeholder='', data-html='1', data-toggle='true', bs-options='item.value as item.label for item in vm.statuses', bs-select='')
            span.caret
          // delete
          button.m-select(  type='button', placeholder='')
            span.m-select.m-selected-bucket

      // Все посты
      .ng-scope
        .modal-header.modal-header-lists
          h3#gridSystemModalLabel.modal-title Все посты
          
        style.
          .btn-group, #gridSystemModalLabel{display: inline-block;}
          .modal-header-lists{border-bottom:none;}
          .modal-body-lists{padding: 0 0px;}
        .modal-body.modal-body-lists
          form.form-horizantal.ng-pristine.ng-valid
            table.table.lists
              tr
                th Тема
                th Создана
                th Ответы
                th Владелец / Автор
                th
              tr(ng-repeat='post in posts')
                td
                  span.tema(id='{{post._id}}', ng-click='selectTema(post._id)') {{post.tema}}
                td {{post.date | date:dateFormat1}}
                td {{post.answer | date:dateFormat1}}
                td {{post.creatorName}}
                td
                  i.fa.fa-star-o.star(aria-hidden='true')
      // end Все посты
      // Новое сообщение
      .modal-header
        h3#gridSystemModalLabel.modal-title Новое сообщение
        span.text-danger.text-error {{postError}}
      .modal-body
        form.form-horizantal.ng-pristine.ng-valid
          .form-group
            label.col-sm-2.control-label.newPost(for='inputEmail3') Получатели
            .col-sm-10.newPost
              style.
                .autocomplete{margin: 0 0;}
                .employee, .all, .contractor{text-align: left;}
                .all{background: #d3d3bc;}
              .dropdown2
                #dLabel.autocomplete.autocomplete-div(data-toggle='dropdown')
                  span.autocomplete-text Имя, должность, почта или стрелка вниз для ручного поиска
                  // Выбранные юзеры
                  .selected-item(ng-repeat='user in selectedUers')
                    table.selected-item-table
                      re
                        td
                          .selected-item-avatar
                            img.img-rounded(src='{{user.avatar}}', width='100%', height='100%', alt='')
                        td
                          span.selected-item-name {{user.name}}
                          br
                          span.selected-item-profesion программист
                        td
                          span.selected-item-close(ng-click='deleteUser(user._id)')
                            i.fa.fa-times(aria-hidden='true')
                  // end Выбранные юзеры
                  input.autocomplete-input(type='text', placeholder='Имя, должность, почта или стрелка вниз для ручного поиска')
                span#dLabel.caret.autocomplete-caret(type='button', data-toggle='dropdown')
                // dropdown-menu все юзеры
                ul.dropdown-menu(aria-labelledby='dLabel')
                  // Получатели все юзеры
                  .autocomplete-allUsers
                    .comment(ng-repeat='user in allUsers track by $index', ng-click='addUserToSelected(user._id)')
                      table.comment-table.autocomplete-table.table-hover
                        tr
                          td
                            .autocomplete-photo
                              img.img-thumbnail(src='{{user.avatar}}', width='100%', height='100%', alt='')
                          td
                            .comment-info
                              p.comment-name.autocomplete-name {{user.name}}
                              p.comment-date.autocomplete-who  ---------- 
                  // end Получатели все юзеры
                  // Юзеры по котегориям
                  .autocomplete-all
                    ul.ui-sdf-autocomplete-dropdown-filters
                      li.all-li
                        .all.filter-parent.ui-sdf-autocomplete-filter
                          .filter-shadow
                          span Все
                      li.ui-sdf-autocomplete-filter-item.selected-filter
                        .employee.filter-parent.ui-sdf-autocomplete-filter
                          .filter-shadow
                          span
                            i.fa.fa-university(aria-hidden='true')
                            |                                                                             Сотрудники
                      li.ui-sdf-autocomplete-filter-item
                        .contractor.filter-parent.ui-sdf-autocomplete-filter
                          .filter-shadow
                          span
                            i.fa.fa-suitcase(aria-hidden='true')
                            |                                                                             Все Клиенты
                      li.ui-sdf-autocomplete-filter-group
                        span.filter-child.employee.ui-sdf-autocomplete-filter-group2 Отделы
                      li.ui-sdf-autocomplete-filter-item(ng-repeat='name in otdel')
                        .filter-child.employee.ui-sdf-autocomplete-filter
                          .filter-shadow
                          span {{name.name}}
                      li.ui-sdf-autocomplete-filter-group
                        span.filter-child.employee.ui-sdf-autocomplete-filter-group2 Сотрудники по должности
                      li.ui-sdf-autocomplete-filter-item(ng-repeat='name in sotrudniki')
                        .filter-child.employee.ui-sdf-autocomplete-filter
                          .filter-shadow
                          span {{name.name}}
                      li.ui-sdf-autocomplete-filter-group
                        span.ui-sdf-autocomplete-filter-group2 Пользовательские группы
                  // end Юзеры по котегориям
                // end dropdown-menu все юзеры
          br
          br
          br
          br
          .form-group
            label.col-sm-2.control-label.newPost(for='inputEmail3') Тема
            .col-sm-10.newPost
              input.form-control(type='text', placeholder='Тема', ng-model='postTema')
          br
          br
          .form-group
            label.col-sm-2.control-label.newPost(for='inputEmail3') Содержание
            .col-sm-10.newPost
              .write-comment
                text-angular(ng-model='htmlVariable', ta-toolbar="[['h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'p', 'pre', 'quote','bold', 'italics', 'underline', 'ul', 'ol', 'redo', 'undo'],['justifyLeft','justifyCenter','justifyRight','justifyFull','indent','outdent'], ['insertImage', 'insertLink', 'insertVideo', 'wordcount', 'charcount']]")
          br
          br
          br
          button#saveMessage.btn.btn-success(ng-click='addPost()', type='button') Сохранить
        br
        p.text-muted Данное сообщение увидите только вы и адресат
      // end Новое сообщение'
      // Пост
      .ng-scope
        .modal-header
          h3#postTema.modal-title {{getPostTema}}
          br
          br
          #postText.bg-success.post
        br
        br
        .modal-body.modal-body-lists
          .comment
            table.comment-table
              tr
                td
                  .comment-photo
                    img.img-thumbnail(src='{{userAvatar}}', width='100%', height='100%', alt='')
                td
                  // -- Комментарий write --
                  .comment-info
                    p.comment-name {{userName}}
                    .write-comment.addComment
                      p Комментарий
                      text-angular(ng-model='htmlVariable', ta-toolbar="[['h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'p', 'pre', 'quote','bold', 'italics', 'underline', 'strikeThrough', 'ul', 'ol', 'redo', 'undo'],['justifyLeft','justifyCenter','justifyRight','justifyFull','indent','outdent'], ['insertImage', 'insertLink', 'insertVideo', 'wordcount', 'charcount']]")
                      br
                      button#sendComment.btn.btn-success(type='button', ng-click='addComment()') Отправить
                      span.text-danger.text-error {{commentError}}
                  // end -- Комментарий write --
          // Комментарий show
          .comments
            hr
            .comment(ng-repeat='comment in comments')
              table.comment-table
                tr
                  td
                    .comment-photo
                      img.img-thumbnail(src='https://encrypted-tbn1.gstatic.com/images?q=tbn:ANd9GcS7XmgLfLPpB2aO_I4-7ir7KkZfz4hUMXSuAgPdJ44eFGWm31E-', width='100%', height='100%', alt='')
                  td
                    .comment-info
                      p.comment-name {{comment.userName}}
                      p.comment-date {{comment.date | date:dateFormat2}}
                      p.comment-text(ng-bind-html='comment.text')
              hr
          // end Комментарий show
      // end Пост